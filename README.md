# online-book-review

1. Prerequisites

- Node.js (v14+)
- npm

2. Install dependencies:
   npm init -y
   npm install express body-parser express-session jsonwebtoken bcrypt axios

3. Run the server:
   node server.js
